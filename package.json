{
  "name": "cvgeanetor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test --experimental-strip-types tests/ats-utils.test.mjs",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:install": "playwright install chromium"
  },
  "dependencies": {
    "@geajs/core": "^1.0.15"
  },
  "devDependencies": {
    "@geajs/vite-plugin": "^1.0.15",
    "@playwright/test": "^1.58.2",
    "typescript": "^5.9.3",
    "vite": "^8.0.0"
  }
}
